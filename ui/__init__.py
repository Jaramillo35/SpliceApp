"""Streamlit UI layer.

Thin presentation code that calls into the ``splice`` package. Modules here may
import ``streamlit``; ``splice`` never does. Business logic belongs in ``splice``,
not here.
"""
