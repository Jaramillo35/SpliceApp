"""Splice — Streamlit-independent core for the DTx/SECR engineering toolkit.

Every sub-package here is importable and testable without Streamlit. The UI layer
(``ui/``) depends on this package; this package never imports ``streamlit``.
"""
