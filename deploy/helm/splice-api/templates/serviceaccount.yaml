{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "splice-api.serviceAccountName" . }}
  labels:
    {{- include "splice-api.labels" . | nindent 4 }}
{{- end }}
