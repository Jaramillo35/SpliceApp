apiVersion: v1
kind: Service
metadata:
  name: {{ include "splice-api.fullname" . }}
  labels:
    {{- include "splice-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "splice-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
