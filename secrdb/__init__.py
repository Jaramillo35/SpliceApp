"""SECR Database — a searchable history of wiring-harness engineering changes.

The deterministic engines and persistence live in the ``splice`` package, which
this app imports; ``secrdb`` holds what is specific to this product: its
configuration, its user interface, and the local AI assistant.
"""
